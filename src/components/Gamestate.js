// Define a constant object called GameState
const GameState = {
    playerXwins: 0,     // Represents the state when player X wins
    playerOwins: 1,     // Represents the state when player O wins
    draw: 2,            // Represents the state when the game ends in a draw
    inProgress: 3,      // Represents the state when the game is still in progress
};

// Export the GameState object as the default export of this module
export default GameState;